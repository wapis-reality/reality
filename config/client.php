<?phpclass CLIENTS{    static $site = array(           "limit_block" => 3,           "expire_password" => 1,           "forgott_expire_password" => 1        );    static function get($prop = null)    {        $group = 'site';        return self::${$group}[$prop];    }    static function exist($prop = null)    {        $group = 'site';        if (!$group)            return false;        return isset(self::${$group}[$prop]);    }}//session_cache_limiter("must-revalidate");session_start();//header('P3P: CP="NOI ADM DEV PSAi COM NAV OUR OTRo STP IND DEM"');